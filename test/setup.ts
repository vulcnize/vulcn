// Test setup file
// Add any global test configuration here

// Increase test timeout for browser tests
import { beforeAll, afterAll } from "vitest";

beforeAll(() => {
  // Global setup
});

afterAll(() => {
  // Global cleanup
});
